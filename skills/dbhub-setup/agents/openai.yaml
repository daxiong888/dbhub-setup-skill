interface:
  display_name: "DBHub Setup"
  short_description: "为 Codex 安全配置多环境、多数据库 DBHub MCP"
  default_prompt: "Use $dbhub-setup to configure project-level DBHub MCP for multiple databases and environments, with macOS Keychain-backed credentials, lazy read-only tools, and no plaintext passwords."
